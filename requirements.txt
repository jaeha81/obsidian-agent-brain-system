# Obsidian Agent Brain System — Python Dependencies
# Updated: 2026-05-25

# ── AI / Claude API ──────────────────────────────────────
anthropic>=0.28

# ── AgentBus / legalize-mcp ──────────────────────────────
mcp>=1.0

# ── Discord 봇 ───────────────────────────────────────────
discord.py[voice]>=2.3

# ── 환경·설정 ────────────────────────────────────────────
python-dotenv>=1.0
pyyaml>=6.0

# ── HTTP / 스크래핑 ──────────────────────────────────────
requests>=2.31
playwright>=1.44          # 웹 자동화 수집 (collection_pipeline)

# ── 파일 감시 ────────────────────────────────────────────
watchdog>=4.0             # RAW inbox 실시간 감시 (knowledge_distiller --watch)

# ── 스케줄링 ─────────────────────────────────────────────
schedule>=1.2             # collection_scheduler 내부 루프

# ── 유틸리티 ─────────────────────────────────────────────
# feedparser: pip install feedparser  (선택, 뉴스 수집)
# openai-whisper: pip install openai-whisper  (선택, 음성 전사)
# FFmpeg 필수 (voice): winget install ffmpeg  또는  choco install ffmpeg

# ── 외부 CLI (별도 설치) ─────────────────────────────────
# legalize-cli: npm install -g legalize-cli  OR  pipx install legalize
# graphify: pip install graphify
